name: flutter_absolute_path_example
description: Demonstrates how to use the flutter_absolute_path plugin.
publish_to: 'none'

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_absolute_path:
    path: ../

flutter:

  uses-material-design: true
